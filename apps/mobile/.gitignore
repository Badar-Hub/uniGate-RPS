# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native (generated by prebuild — this app is Expo Go / EAS managed)
/ios
/android
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# local env files (only .env.example is committed — see root .gitignore)
.env*.local

# Store credentials never enter git (docs/deployment.md)
play-service-account.json
*.keystore
*.jks
keystore.properties
# Deployment-specific CA bundled by plugins/with-network-security.js
/certs/
